import { PriceListProductDetails } from './price-list-product-details';

describe('PriceListProductDetails', () => {
  it('should create an instance', () => {
    expect(new PriceListProductDetails()).toBeTruthy();
  });
});
