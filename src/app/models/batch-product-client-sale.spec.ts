import { BatchProductClientSale } from './batch-product-client-sale';

describe('BatchProductClientSale', () => {
  it('should create an instance', () => {
    expect(new BatchProductClientSale()).toBeTruthy();
  });
});
