import { ProductProductDiscount } from './product-product-discount';

describe('ProductProductDiscount', () => {
  it('should create an instance', () => {
    expect(new ProductProductDiscount()).toBeTruthy();
  });
});
