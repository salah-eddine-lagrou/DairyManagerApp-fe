import { EquipementCategory } from './equipement-category';

describe('EquipementCategory', () => {
  it('should create an instance', () => {
    expect(new EquipementCategory()).toBeTruthy();
  });
});
