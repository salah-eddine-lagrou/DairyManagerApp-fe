import { BatchProductStock } from './batch-product-stock';

describe('BatchProductStock', () => {
  it('should create an instance', () => {
    expect(new BatchProductStock()).toBeTruthy();
  });
});
