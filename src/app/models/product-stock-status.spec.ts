import { ProductStockStatus } from './product-stock-status';

describe('ProductStockStatus', () => {
  it('should create an instance', () => {
    expect(new ProductStockStatus()).toBeTruthy();
  });
});
